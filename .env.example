# Frontend environment variables for Vite
# Set the API base URL when not using the dev proxy.
# Example for direct calls (no proxy):
# VITE_API_BASE_URL=http://localhost:5000

# When using the Vite dev proxy (default in vite.config.ts), you can leave it unset
# and the frontend will call '/api/*' which gets proxied to the backend.
